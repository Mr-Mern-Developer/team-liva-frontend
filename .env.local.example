# Copy to .env.local

# Base URL of the Express API (no trailing slash).
NEXT_PUBLIC_API_URL=https://teamliva-backend.vercel.app/api

# Canonical site URL — used for metadataBase, sitemap and OpenGraph URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
